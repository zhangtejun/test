server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri:
          password:
          username:
          search-paths: config # 多个目录以逗号分隔